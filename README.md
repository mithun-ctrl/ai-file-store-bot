# ai-file-store-bot

File store bot with regex metadata extraction and searchable deep links.

## Commands

- `/search <query>`: Search stored files and get paginated results (10 buttons per page).
- `/start <linkId>`: Fetch files from a stored deep link.
